# Agent Usage 性能修复

范围：修复本次只读性能审查的全部 6 项问题。保留会话/Agent/日期统计、迟到及修订记录、首次/重复归因、完整历史账本与来源区分；不引入生产依赖，不提交、推送、部署或使用真实 Provider。完成修复、回归与性能复测之后再研究 OmniRoute。

1. MCP 完成路径：主键读取 invocation，禁止加载历史 exposure；回归覆盖完成状态和读取规模。
2. 内存生命周期：相同词表共享引擎及纯计数缓存；MCP ticket 在维护/删除后回收，保留收尾事件。验证跨 profile 元数据、不同词表、Session 隔离、旧 token 拒绝。
3. HTTP 缓存：只写新增、变更及淘汰项；验证无变化时零缓存行写入，以及修订/淘汰/事务回滚。
4. 查询：维护可重建首次暴露索引；数据库聚合、过滤、分页；前端翻页不重载汇总。验证历史首次、迟到/修订/删除、排序游标、来源与空值语义；读行预算回归。
5. 文件采集：增量 JSONL 游标，解析器状态/累计基线/半行恢复，保留替换检测；批次与 checkpoint 原子提交。验证重启、失败恢复、重复采集、修订、截断、取消与拒绝计数。
6. 运行聚焦测试、全量测试、typecheck、build、diff-check；用同一 60,000 暴露、5,000 行日志和 2,048 缓存基准复测；独立审查后处理发现。

执行记录：继续当前 codex/agent-usage-observability 工作区，保留之前全部未提交修改。已有性能报告是修复依据；公共统计合同见 docs/agent-usage.md。前端只调整请求依赖，不更改视觉设计。
